// bitwise operators
// AND 
//** when we and 1 with any no digits will remain same

// OR

// XOR if and only if exclusive or
// only one should be true 
// if we xor any number with 1 we get complement of that number
// if we xor any number with 0 we get that number
// if we xor any number with that number we get 0

// complement 

// left shift operator (<<)
// a << 1 = 2a
// a << b = 2^b a

// right shift operator (>>)
// a >> 1 = a/2
// a >> b = a / 2^b

// number systems : decimal (10) bnary (2) octal (8) hexadecimal(16)